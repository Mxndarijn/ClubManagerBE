query MyQuery {
    userQueries {
        getMyProfile {
            email
            fullName
            id
            language
            role {
                name
            }
        }
    }
}